services:
  box:
    build: .
    ports:
      - "8080:8080"
    environment:
      OPENBOT_PASSWORD: ${OPENBOT_PASSWORD:-openbot}
      PORT: "8080"
      PWA_DIR: /app/pwa
    restart: unless-stopped
